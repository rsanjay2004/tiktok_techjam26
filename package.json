{
  "name": "autonomous-ml-research-triage",
  "version": "1.0.0",
  "private": true,
  "description": "Autonomous ML requirement detection and lightweight classifier training demo.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check src/app.js"
  },
  "engines": {
    "node": ">=22"
  }
}
